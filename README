ProxyPing
=========

A simple rails plugin that adds an action that your reverse proxy/load balancer can ping and not leave a rails log entry.

You will need to add a route like so to your config/routes.rb:

map.route '/proxy_ping', :controller => 'proxy_ping', :action => 'ping'

Copyright (c) 2008 Hugh Evans, released under the MIT license
